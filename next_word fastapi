app = FastAPI()
predictor = NextWordPredictor()

class InputText(BaseModel):
    text: str
    top_k: int = 5

@app.post("/predict")
def predict_next_word(input_data: InputText):
    prediction = predictor.predict(input_data.text, input_data.top_k)
    return {"next_word": prediction}
